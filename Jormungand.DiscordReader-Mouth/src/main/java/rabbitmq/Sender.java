package rabbitmq;

public class Sender {
    /*TODO:
    - create constructor with host, username and pw
    - create methods with exchange (similar to topics)
    - assure that exchanges exist even if they aren't created beforehand (possibly scriptable via docker, idk)
     */
    private String adress="";

}
